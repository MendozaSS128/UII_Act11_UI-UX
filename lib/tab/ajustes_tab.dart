import 'package:flutter/material.dart';

class AjustesTap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("AJUSTES TAB"),
      ),
    );
  }
}
