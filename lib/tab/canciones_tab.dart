import 'package:flutter/material.dart';

class CancionesTap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("CANCIONES TAB"),
      ),
    );
  }
}
