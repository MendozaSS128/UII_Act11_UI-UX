import 'package:flutter/material.dart';

class AlbumsTap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("ALBUMS TAB"),
      ),
    );
  }
}
